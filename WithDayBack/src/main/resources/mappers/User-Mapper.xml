<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.localandsocialloginback.user.repository.UserMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, name, email, phone, role, gender, birth_date)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{role}, #{gender}, #{birthDate})
    </insert>

    <select id="existsByUsername" resultType="boolean">
        SELECT IF(COUNT(*) > 0, true, false)
        FROM users
        WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT IF(COUNT(*) > 0, true, false)
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findByUsername" resultType="com.springboot.localandsocialloginback.user.entity.User">
        SELECT user_id as userId, username, password, name, email, phone, role, gender, birth_date as birthDate
        FROM users
        WHERE username = #{username}
    </select>

</mapper>