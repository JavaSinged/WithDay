<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.localandsocialloginback.user.repository.EmailVerificationMapper">

    <insert id="save">
        INSERT INTO email_verification (email, code, expiry_date)
        VALUES (#{email}, #{code}, #{expiryDate})
            ON DUPLICATE KEY UPDATE
                                 code = #{code},
                                 expiry_date = #{expiryDate}
    </insert>

    <select id="findById" resultType="com.springboot.localandsocialloginback.user.entity.EmailVerification">
        SELECT email, code, expiry_date as expiryDate
        FROM email_verification
        WHERE email = #{email}
    </select>

    <delete id="deleteById">
        DELETE FROM email_verification
        WHERE email = #{email}
    </delete>

</mapper>